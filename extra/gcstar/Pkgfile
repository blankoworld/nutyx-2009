# Description: Gestionnaire de collections
# URL:http://www.gcstar.org/index.fr.php
# Maintainer: NuTyX packager team
# Packager: lesibel at free dot fr
# Depends on: p5-gtk2, p5-libwww, p5-xml-simple, p5-html-parser, p5-html-tagset, p5-archive-zip 
name=gcstar
version=1.5.0.beta1
release=2

source=( http://download.gna.org/$name/$name-$version.tar.gz \
$name.desktop \
GCAllocine.pm.patch \
GCPluginsBase.pm.patch)

build() {
  cd $name

  # installation
  ./install --prefix=$PKG/usr

  # corrections diverses
  ## pour correction de l'extraction d'allocine
  patch -p1 $PKG/usr/lib/gcstar/GCPlugins/GCfilms/GCAllocine.pm < $SRC/GCAllocine.pm.patch
  ## pour une correction mineure sur les plugins
  patch -p1 $PKG/usr/lib/gcstar/GCPlugins/GCPluginsBase.pm < $SRC/GCPluginsBase.pm.patch

  # ajout d'ic�nes divers
  install -D -m644 share/gcstar/icons/gcstar_32x32.png \
  $PKG/usr/share/pixmaps/gcstar.png &&  \
  cp -R share/applications $PKG/usr/share

  # ajout d'un élément dans le menu
  mkdir -p $PKG/usr/share/applications
  install -m 644 $SRC/$name.desktop $PKG/usr/share/applications/
}

